targets: [android]
includes: [SDL2-2.0.5/include]
android:
  lib: libSDL2.a
  ndk:
    runtimes: [c++, gnustl]
    platforms: [12, 21]
example: |
  #include <SDL.h>
  void test() {
    SDL_Window *window;                    
    SDL_Init(SDL_INIT_VIDEO);              
  }
